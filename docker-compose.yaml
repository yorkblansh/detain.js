version: '3.8'

services:
  detain:
    extra_hosts:
      - 'host.docker.internal:host-gateway'
    # network_mode: "host"
    container_name: detain-dev
    build:
      context: ./
      dockerfile: ./Dockerfile.dev
    ports:
      - 5172:5172
    volumes:
      - ./:/app
    tty: true
    stdin_open: true
    restart: on-failure
